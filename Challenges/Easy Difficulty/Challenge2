'''
Challenge #2: Create a function to show odd numbers between 
a starting number and a finishing number
'''

def odd_numbers():
  first_number = int(input("What number do you choose to begin with? "))
  end_number = int(input("What number do you choose to end with? "))
  x = first_number + 1
  y = end_number + 1
  if (first_number % 2 == 1):
    print(*range(first_number, y, 2))
  else:
    print(*range(x, y, 2))
  
  odd_numbers()