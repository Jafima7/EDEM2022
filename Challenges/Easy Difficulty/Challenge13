'''
Challenge 13: Create a function to calculate the volume of a cylinder,
using the function to calculate the area of a circle (from Challenge 12). The only
input for the cylinder volume calculation function is the height of the cylinder.
'''

import math

def area_circle(radius):
  area = math.pi * (radius ** 2)
  return area

def volume_cylinder(radius, height):
  volume = area_circle(radius) * height
  print(volume)

area_circle()
volume_cylinder()