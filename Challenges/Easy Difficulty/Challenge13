'''
Challenge 13: Create a function to calculate the area of a cylinder,
using a function to calculate the area of a circle. The user chooses
the parameters: radius of the circle, and height of the cylinder.
'''

import math

def area_circle(radius):
  area = math.pi * (radius ** 2)
  return area

def area_cylinder(radius, height):
  area_cylinder = (2 * area_circle(radius)) + (2 * math.pi * radius * height)
  print(area_cylinder)